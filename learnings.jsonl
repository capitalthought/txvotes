{"ts": "2026-05-26T20:30:00Z", "skill": "manual", "type": "pitfall", "key": "date-sensitive-tests-must-freeze-clock-pre-election", "insight": "The worker short-circuits all election logic after ELECTION_DAY (2026-03-03): runDailyUpdate and runCountyRefresh return { skipped: true } (no countyLog/countiesRefreshed/county fields), and the guide/summary/stream endpoints return 410 Gone. Any test asserting pre-election behavior MUST freeze the clock or it becomes a time bomb that goes red the day after the election. Use vi.useFakeTimers({ now: new Date('2026-02-20T12:00:00Z'), toFake: ['Date'] }) in beforeEach + vi.useRealTimers() in afterEach. CRITICAL: pass toFake:['Date'] when the code under test calls sleep()/setTimeout (e.g. runCountyRefresh sleeps 3-5s between counties) — faking setTimeout too makes those sleeps hang forever and the test times out. Date-only faking freezes the clock while leaving real timers intact. Phase can also be forced via KV (site_phase:tx) or ?test_phase= query param, which is preferable for tests specifically exercising post-election behavior.", "confidence": 10, "source": "observed", "files": ["worker/tests/updater.test.js", "worker/tests/streaming.test.js", "worker/tests/post-election.test.js", "worker/tests/smoke.test.js", "worker/tests/stats.test.js", "worker/src/updater.js"], "evidence": "2026-05-26 — full CI suite had been red since the March 3 primary; 19 tests across 5 files failed with 'expected undefined to be defined' / 'expected 410 to be 400'. Fixed by freezing the clock per-block (commit abd7347). 2219/2219 tests green after."}
{"ts": "2026-05-26T21:05:00Z", "skill": "manual", "type": "reference", "key": "txvotes-cloudflare-deploy-coordinates", "insight": "txvotes.app lives on the **joshshop** Cloudflare account (Github@joshshop.com, account_id ac5a9971e6d58775db8f543db1489403), zone_id 9de4d7e2ec8e472ea8c4bc3be1ab1f5d — NOT capitalfactory. To deploy: CLOUDFLARE_API_TOKEN = 1P 'Cloudflare — Workers Deploy (joshshop)' (item no3h45wmxqobmnz2clprrore7a, field api_token) + CLOUDFLARE_ACCOUNT_ID from that item's account_id field, then `npx wrangler deploy -c wrangler.txvotes.toml`. DNS edits: 1P 'Cloudflare — DNS Edit (joshshop)' (wknfj2uzjabdasrjpq7v3izvle, field api_token). The 'TXVotes Cloudflare API Token' 1P item is READ-only scoped (can read zones, cannot edit DNS). Host pattern for new subdomains served by the worker: proxied CNAME -> txvotes.app (matches www), then add the route to wrangler.txvotes.toml.", "confidence": 10, "source": "observed", "files": ["worker/wrangler.txvotes.toml"], "evidence": "2026-05-26 — deploying old.txvotes.app demo. Verified token scopes by API: TXVotes token got 'Authentication error' on dns_records; joshshop DNS Edit + Workers Deploy tokens worked. Deploy succeeded, version 7f28959a."}
{"ts": "2026-05-26T21:06:00Z", "skill": "manual", "type": "pitfall", "key": "anthropic-txvotes-key-spend-cap", "insight": "The TX Votes Anthropic API key has a monthly spend cap that was HIT — live guide generation returns 'API error 400: You have reached your specified API usage limits. You will regain access on 2026-06-01 at 00:00 UTC.' The worker correctly streams an event:meta then event:error. Anything depending on live guide generation (incl. the old.txvotes.app demo) will fail until the cap resets June 1 or is raised in the Anthropic console for the TX Votes key.", "confidence": 9, "source": "observed", "files": ["worker/src/pwa-guide.js"], "evidence": "2026-05-26 — guide-stream on old.txvotes.app returned the usage-limit 400 while ballot data + UI served fine."}
